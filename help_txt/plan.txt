

fichier de config = regle on fait quoi si qqn se co a ce port




1 - on recupere info fichier de config (port pr l'instant)

2 - on stock ce port dans un vector 

3 - on creer socket canal de co -> attache port au canal (bind) -> listen

4 - on stock le fd dans un vector _serverfds (tout les connecteer)

5- on wait clients 

    on use epoll (il surveiller tout les fds en meme temps)
     1 ) epoll create1 (creer le surveillant)
     2 ) Eepoll ct1 (instruction des socket a surveiller) - pour tous
        stockage dans un tab event des fd ? (type d'event etc.)
     3 ) epoll wait (attend activite ) return le nb de event en cours et les fd concerner

    mtn je lance un while de chaque fd connecter (recuperer avec n)
    et je dois checker si (chaque fd de n ont deja ete ouvert ou nn)
    enfaite c pas ca 
        en gros : au debut tu as des port de co (le client tape) et on accept
        et la on lui creer un port de communication
        - donc si c un port connu -> futur client on lui creer un fd
    1 ) on ajoute a epoll le canal client en disant ecoute
    2 ) on gere le fait que si c un client
        on dois lire sa requete ensuite on parsera.. 



    4 )parser la requete 
        use getline ? 
        




game plan : 

prochaines etapes 


Readclient -> recv (recupere requete) : DONE 

1) parser la requetes
2) traiter (get / post /delete)
3 ) construire la reponse 
4) send()



comprehension des types de request : 

une requete aura toujours : 
    la premiere ligne : Type de requte / le path et la version http
    la seconde ligne (header) : a quel serveur envoyer
    une ligne vide (pr separer header du body ) : 

get : 
     je tappe une adresse genre google.com. le nav envoie une requete au serveur qui gere ládresse..
     il recupere une ressource 
delete : 
        supprime cette ressource : 
            exemple amazon et toi. tu veux supprimer une adresse de livraison -> requete delete
post : 
    il envoie les donnes inverse de get.. 
     exemple : remplis un formulaire et tu clique sur envoyer.. ou une photo que tu upload. il ajoute de l'info

// -------------------------------------------------

GAME PLAN WEBSERV

FONDATION (fait ✅)
- config : parser les ports
- socket → bind → listen → stocker fd
- epoll : create → ctl → wait
- accept les clients
- readClient → recv (récupère requête) ✅

PILIER 1 : répondre (le plus urgent)
1) send() une réponse bidon "Hello"     → curl/navigateur reçoit qqch
2) parser la requête (méthode, chemin, headers)

3) construire une vraie réponse HTTP
4) servir un fichier statique (GET → renvoie une page html)

PILIER 2 : robustesse HTTP
5) codes d'erreur (404, 403, 405, 400, 413...)
6) pages d'erreur


PILIER 3 : les méthodes
7) GET complet
8) POST (+ upload de fichiers)
9) DELETE




// ici on fera un arret pour tout checker 


PILIER 4 : config avancée
10) location, root, index
11) plusieurs server blocks
12) client_max_body_size

PILIER 5 : le gros morceau
13) CGI (exécuter scripts php/python)

// fin de projet et ici peaufinnage

PILIER 6 : finitions
14) non-bloquant strict (recv/send partiels)


15) timeouts, connexions qui coupent
16) ne jamais crash
17) tests navigateur réels


// -------------------------------------------------

GAME PLAN WEBSERV

FONDATION ✅ FAIT + TESTÉ
- config : parser les ports
- socket → bind → listen → stocker fd
- epoll : create → ctl → wait
- accept les clients
- readClient → recv (récupère requête)

PILIER 1 : répondre ✅ FAIT + TESTÉ
1) send() une réponse au client
2) parser la requête (méthode, chemin, headers)
3) construire une vraie réponse HTTP
4) servir un fichier statique (GET → page html)

PILIER 2 : robustesse HTTP ✅ FAIT + TESTÉ
5) codes d'erreur (404, 403, 405, 400, 413, 500...)
6) pages d'erreur par défaut

PILIER 3 : les méthodes ✅ FAIT + TESTÉ
7) GET complet (+ protection ..)
8) POST (+ upload de fichiers)
9) DELETE

// ───────── POINT D'ARRÊT : bloc 1 validé ✅ ─────────


PILIER 4 : NON-BLOQUANT (obligatoire, grade 0 sinon) ❌ À FAIRE
   ⚠️ remonté ici car : obligatoire + touche la boucle epoll
      + plus facile sur base simple qu'après CGI
14) mettre tous les sockets en non-bloquant (fcntl O_NONBLOCK)
15) buffer de lecture par client (recv partiels)
16) buffer d'écriture par client (send partiels)
17) attendre la requête complète avant de parser
18) nettoyer un client qui coupe la connexion

PILIER 5 : config avancée ❌ À FAIRE
19) location, root, index
20) plusieurs server blocks
21) client_max_body_size (depuis la config)
22) méthodes autorisées par route

PILIER 6 : CGI (le gros morceau) ❌ À FAIRE
23) exécuter scripts php/python

PILIER 7 : finitions ❌ À FAIRE
24) timeouts, connexions qui coupent
25) ne jamais crash (tests de robustesse)
26) tests navigateur réels (Firefox)
27) README (obligatoire au rendu)